export interface Items {
    id: string
    name: string
    price: number
    img: string
}

export interface Cart {
    id: string
    name: string
    price: number
    img: string

    quantity: number
}